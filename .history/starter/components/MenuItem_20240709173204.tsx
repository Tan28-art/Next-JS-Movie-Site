import React from 'react'

interface MenuItemProps {
    title: string;
    icon: string;
  }
  

export default function MenuItem({ title, disabled }: MenuItemProps) {
  return (
    <div>
      MenuItem
    </div>
  )
}
